import React, { Component } from 'react';
import { Button } from 'react-bootstrap';

class Home extends Component {
  render() {
    return (
      <div>
        <Button variant="primary">
          Home to professional workers.
        </Button>
      </div>
    );
  }
}

export default Home;
